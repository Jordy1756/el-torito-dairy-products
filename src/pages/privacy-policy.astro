---
import Layout from "@layouts/Layout.astro";
import Navbar from "@components/layout/Navbar.astro";
import Footer from "@components/layout/Footer.astro";
import Policies from "@components/sections/policies/Policies.astro";

import { basics, privacyPolicySection } from "@data";

const { name } = basics;
const { title, description, content } = privacyPolicySection;
---

<Layout title=`Política de privacidad de ${name}`>
    <header>
        <Navbar />
    </header>
    <main>
        <Policies title={title} description={description} content={content} />
    </main>
    <Footer />
    <!-- <script src="../scripts/pages/privacyPolicy.ts"></script> -->
</Layout>

<style>
    header {
        position: sticky;
        top: 0;
        left: 0;
        display: flex;
        justify-content: center;
        padding: 0.5rem 1rem;
        border-bottom: 1px solid var(--neutral-300);
        background-color: var(--neutral-50);
        z-index: 999;
    }

    main {
        align-self: center;
        padding: 3rem 1rem;
        flex: 1;
        width: min(100%, calc(var(--max-width) / 1.5));
    }
</style>
